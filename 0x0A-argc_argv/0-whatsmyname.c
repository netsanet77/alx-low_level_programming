#include <stdio.h>
#include <stdlib.h>
/**
 * main -  entry point
 * @argc: the size of the array
 * @argv: given array
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[argc - 1]);
return (0);
}
