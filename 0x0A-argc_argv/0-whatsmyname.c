#include <stdio.h>
/**
 * main -  entry point
 * @argc: the size of the array
 * @args: given array
 * Return: Always 0
 *
 */
int main(int argc, char *args[])
{
printf("%s\n", args[argc - 1]);
return (0);
}
