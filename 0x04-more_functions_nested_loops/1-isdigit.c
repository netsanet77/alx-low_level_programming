#include "main.h"
/**
 * _isdigit - check whether the character is digit or not
 *@c: character to be check
 *
 * Return: 1 or 0
 */
int _isdigit(int c)
{
if (c <= 9 && c >= 0)
{
return (1);
}
else
{
return (0);
}
}
